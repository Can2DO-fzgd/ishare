<div class="panel panel-default">
  <div class="panel-heading">
    <div class="pull-right">
      {% if canManage %}
        <a href="#modal" data-toggle="modal" data-url="{{ path('product_announcement_add',{productId:product.id}) }}" class="btn btn-link btn-xs"><span class="glyphicon glyphicon-plus"></span> 添加公告</a>
      {% endif %}
    </div>
  	<h3 class="panel-title">产品公告</h3>
  </div>

  <div class="panel-body">
    <ul class="media-list announcement-list">
      {% for announcement in announcements %}
        <li class="media">
          <div class="media-body">
            <p>
              {% if canTake %}
                <a id="product-buy-btn" href="#modal" data-toggle="modal" data-url="{{ path('product_announcement_show', {productId:product.id, id:announcement.id}) }}" >{{ announcement.content|plain_text(40)|default('<span class="text-warning">(请点击查看)</span>')|raw }}</a>
              {% else %}
                <a id="product-buy-btn" href="#modal" data-toggle="modal" data-url="{{ path('product_buy', {id:product.id}) }}" >{{ announcement.content|plain_text(40)|default('<span class="text-warning">(请点击查看)</span>')|raw }}</a>
              {% endif %}
            </p>

            {% if canManage %}
              <div class="clearfix">
                <span class="pull-right text-muted text-sm">{{ announcement.createdTime|date('Y-m-d H:i') }}</span>
                <a class="text-muted text-sm mrm action" href="javascript:;" data-target="#modal"  data-toggle="modal" data-url="{{ path('product_announcement_update',{productId:product.id, id:announcement.id}) }}"><span class="glyphicon glyphicon-pencil"></span> 修改</a>
                <a class="text-muted text-sm action" href="#" data-role="delete" data-url="{{ path('product_announcement_delete',{productId:product.id, id:announcement.id}) }}"><span class="glyphicon glyphicon-trash"></span> 删除</a>
              </div>

            {% endif %}
          </div>
        </li>
      {% else %}
        <li class="empty">暂无公告</li>
      {% endfor %}
    </ul>
  </div>
</div>