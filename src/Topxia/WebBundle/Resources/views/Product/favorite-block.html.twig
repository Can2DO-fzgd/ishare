
    {% if favoriteStatus == 1 %} 
      <a class="mrm " href="{{ path('product_unfavorite', {id:product.id}) }}"  ><i class="glyphicon glyphicon-star"></i> 取消收藏 </a>
    {% elseif favoriteStatus == 0 %}
      <a class="mrm" href="{{ path('product_favorite', {id:product.id}) }}"  ><i class="glyphicon glyphicon-star-empty"></i> 收藏产品 </a>
    {% endif %}