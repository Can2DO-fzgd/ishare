{% embed "TopxiaWebBundle:Bootstrap:panel.html.twig" %}
  {% block heading %}
  <h2>说明书材料</h2>
  {% endblock %}
  {% block body %}

  {% if materials %}
	  <ul>
	  {% for material in materials %}
	    <li>
        {% if app.user and (app.user.id == product.userId or is_granted('ROLE_ADMIN')) %}
          <a class="pull-right material-delete" href="javascript:" data-url="{{ path('product_material_delete', {id:product.id, materialId:material.id}) }}">x</a>
        {% endif %}
        <a href="{{ path('product_material_download', {productId:product.id, materialId:material.id}) }}">{{ material.title }}</a>
        <div class="text-muted">{{ material.description }}</div>
      </li>
	  {% endfor %}
	  </ul>
  {% else %}
    <div class="text-muted">此产品暂无使用说明书材料</div>
  {% endif %}

  {% endblock %}
{% endembed %}