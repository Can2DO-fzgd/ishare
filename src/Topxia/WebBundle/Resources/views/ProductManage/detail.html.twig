{% extends 'TopxiaWebBundle:ProductManage:layout.html.twig' %}

{% block title %}产品详细信息 - {{ parent() }}{% endblock %}

{% set side_nav = 'detail' %}
{% set script_controller = 'product-manage/detail' %}

{% block main %}

<div class="panel panel-default panel-col">
  <div class="panel-heading">产品详细信息</div>
  <div class="panel-body">
    <form id="product-base-form" class="form-horizontal" method="post">
      {{ web_macro.flash_messages() }}

      <div class="form-group">
        <div class="col-md-2 control-label"><label>产品规格</label></div>
        <div class="col-md-8 controls">
          <textarea name="specinfo" rows="10" id="product-about-field" class="form-control">{{ field_value(product, 'specinfo') }}</textarea>
        </div>
      </div>

      <div class="form-group dynamic-collection" id="product-goals-form-group">
        <div class="col-md-2 control-label"><label>产品功能</label></div>
        <div class="col-md-8 controls">
          <ul class="list-group sortable-list" data-role="list" style="margin-bottom:10px;display:none;"></ul>
          <div class="input-group">
            <input id="teacher-input" type="text" data-role="item-input" class="form-control">
            <span class="input-group-btn">
              <button class="btn btn-default" type="button" data-role="item-add" >添加</button>
            </span>
          </div>

          <script type="text/plain" data-role="model">{{ product.efficacyinfo|json_encode|raw }}</script>
          <script type="text/x-handlebars-template" data-role="item-template">
            {% verbatim %}
              <li class="list-group-item clearfix" data-role="item">
                <span class="glyphicon glyphicon-resize-vertical sort-handle"></span>
                {{ this }}
                <input type="hidden" name="efficacyinfo[]" value="{{ this }}">
                <button class="close delete-btn" data-role="item-delete" type="button" title="删除">&times;</button>
              </li>
            {% endverbatim %}
          </script>

        </div>
      </div>

      <div class="form-group" id="product-audiences-form-group">
        <div class="col-md-2 control-label"><label>创新要点</label></div>
        <div class="col-md-8 controls">

          <ul class="list-group sortable-list  dynamic-collection" data-role="list" style="margin-bottom:10px;display:none;"></ul>
          <div class="input-group">
            <input id="teacher-input" type="text" data-role="item-input" class="form-control">
            <span class="input-group-btn">
              <button class="btn btn-default" type="button" data-role="item-add" >添加</button>
            </span>
          </div>

          <script type="text/plain" data-role="model">{{ product.yaodian|json_encode|raw }}</script>
          <script type="text/x-handlebars-template" data-role="item-template">
            {% verbatim %}
              <li class="list-group-item clearfix" data-role="item">
                <span class="glyphicon glyphicon-resize-vertical sort-handle"></span>
                {{ this }}
                <input type="hidden" name="yaodian[]" value="{{ this }}">
                <button class="close delete-btn" data-role="item-delete" type="button" title="删除">&times;</button>
              </li>
            {% endverbatim %}
          </script>

        </div>
      </div>

      <div class="form-group">
        <div class="col-md-8 col-md-offset-2 controls">
            <button class="btn btn-fat btn-primary" id="product-create-btn" type="submit">保存</button>
        </div>
      </div>

      <input type="hidden" name="_csrf_token" value="{{ csrf_token('site') }}">

    </form>
  </div>
</div>

{% endblock %}



