{% extends 'TopxiaWebBundle:MyProduct:layout.html.twig' %}

{% block title %}已收藏 - {{ parent() }}{% endblock %}

{% set tab_nav = 'favorited' %}

{% block main %}
<div class="panel panel-default panel-col">
	  <div class="panel-heading">我的产品</span></div>
	  	<div class="panel-body">

	  		 {% include 'TopxiaWebBundle:MyProduct:nav-pill.html.twig' %}

 {% if products %}
		{{ render(controller('TopxiaWebBundle:Product:productsBlock', {products:products, view: 'list1'})) }}
		{{ web_macro.paginator(paginator) }} 
  {% else %}
    <div class="empty">暂无已收藏的产品</div>
  {% endif %}

</div>
</div>
{% endblock %}