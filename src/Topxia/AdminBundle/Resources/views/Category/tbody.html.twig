<tbody>
  {% for category in categories %}
		<tr id="category-tr-{{ category.id }}">
		  <td>
		    {% for i in 0..(category.depth-1)*2 %}&nbsp;{% endfor %}└─ {{ category.name }}
		    <br>显示序号：{{ category.orderNo }}
		  </td>
		  {#<td>{{ category.sn }}</td>
		  <td>{{ category.orderNo }}</td>#}
		  <td>
		    <a href="javascript:;" class="btn btn-default btn-sm " data-url="{{ path('admin_category_edit', {id:category.id}) }}" data-toggle="modal" data-target="#modal"><i class="glyphicon glyphicon-edit"></i> 编辑</a>
		    {% if group.depth > category.depth %}
		      <a href="javascript:;" class="btn btn-default btn-sm" data-url="{{ path('admin_category_create', {pid:category.id, groupId:group.id}) }}" data-toggle="modal" data-target="#modal"><i class="glyphicon glyphicon-plus"></i> 子类</a>
		    {% endif %}

		  </td>
		</tr>
	{% else %}
	  <tr><td colspan="20"><div class="empty">暂无分类标签记录</div></td></tr>
  {% endfor %}
</tbody>